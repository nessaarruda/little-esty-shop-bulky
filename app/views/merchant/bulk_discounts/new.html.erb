<%= render partial: 'form', locals: {
                                    bulk_discount: @discount,
                                    path: merchant_bulk_discounts_path,
                                    method: :post,
                                    button_text: :submit,
                                    } %>
