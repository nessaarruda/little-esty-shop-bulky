<%= render partial: 'form', locals: {
                                    bulk_discount: @discount,
                                    path: merchant_bulk_discount_path(@merchant, @discount),
                                    method: :patch,
                                    button_text: :submit
                                    } %>
